import Component from "$store/components/product/SellersSelector.tsx";
import type { Props } from "$store/components/product/SellersSelector.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
